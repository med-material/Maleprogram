using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OnePlayerRestriction : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		// check if we are in the Menu scene
        // only activate hands of person that is closest to center (with a max distance)
	}
}
